# U-Net Colorization with Perceptual Loss Project


## 1. what we implemented
We implemented the U-Net architecture and applied it to image colorization on CIFAR-10 in Phase 2 project. In the Phase 3 project, we built on our previous work and implemented perceptual loss introduced in the Unreasonable Effectiveness of Deep Features as a Perceptual Metric paper as a new metric for the colorization task on CIFAR-10 and Oxford Flowers dataset.


## 2. files explanations
### Part 1: 
### UNet.py 
1. in UNet.py we implemented the U-Net architecture introduced in the UNet paper

### training.py 
1. training.py includes the training process where it's using gradient descent, a loss function, and these are all wrapped up in the run_model method
2. it also has a visualize method that generates the input, prediction, and target images for given images in the first batch of the test dataset

### Part 2: 
### data.py
1. build dataset and dataloaders
2. convert CIFAR-10 and Oxford Flowers into (grayscale, color) pairs
3. split into train/validation/test set

### Part 3: 
### perceptual_loss.py
1. the perceptual_loss method implements the perceptual metric introduced in the paper
2. the perceptual_loss_simp method doesn't use the outputs from intermediate layers as in the paper, and instead it uses the final output from the neural network as a simpler version of the paper's original implementation

### Part 4: 
### hyperparameter.py
we implemented experiments on:
1. **perceptual layers**: select specific perceptual layers of CIFAR-10 VGG-19
2. **mse weights**: select different mse weigths for loss function

### flower_hyperparameter.py
we implemented experiments on:
1. **perceptual layers**: select specific perceptual layers of ImageNet VGG-19
2. **mse weights**: select different mse weigths for loss function
3. **learning rate**: select different learning rates for the experiment

## 3. environment setup
1. install dependencies: pip install torch torchvision matplotlib fastprogress


## 4. reproduce the reported figures/tables
### Part 3: 
1. Figure 1. Qualitative comparison of the paper's implementation using intermediate layers' outputs vs. two simpler versions on CIFAR10 is produced by calling the visualize method from training.py, replacing the loss function as needed \
   **command:** ran the model on Jupyter notebook by just running the 2 blocks in run.ipynb
   **expected output:** input, prediction, and target images for each example: the folder /figures/experimental comparisons/CIFAR10 
   
2. Subfigures in Figure 2 are generated during the training process and are directly screenshoted from jupyter notebook

3. Figure 3. Qualitative comparison of the paper's implementation using intermediate layers' outputs vs. two simpler versions on Oxford Flowers is produced by calling the visualize method from training.py, replacing the loss function as needed \
   **command:** ran the model on Jupyter notebook by just running the 2 blocks in run.ipynb
   **expected output:** input, prediction, and target images for each example: the folder /figures/experimental comparisons/Oxford Flowers

4. Figure 4 is generated during the training process and is directly screenshoted from jupyter notebook. 

5. Figure 5. Qualitative results of the paper implementation during different epochs on Oxford Flowers are generated by calling the visualize method during training.
   **command:** just ran the model on Jupyter notebook by just running the 2 blocks in run.ipynb
   **expected output:** input, prediction, and target images for each example: the folder /figures/experimental comparisons/Oxford Flowers/epochs 

### Part 4: 
We run experiments on two datasets and three models. One is CIFAR-10 with CIFAR-10 VGG19. 
1. **command:** python3 hyperparameter.py
2. **expected output:**
   1. training logs for each hyperparameter setting
   2. Part 4 figures/tables:
      1. perceptual_hyperparameter_table.png: summary table of test MSE and perceputal loss of different hyperparameters
      2. mse_comparison_qualitative.png: comparison of the mse weight of the loss
      3. perceptual_layer_qualitative.png: comparison of using different perceptual layers

The other is Oxford 102 flower with ImageNet VGG19.
1. **command:** python3 flower_hyperparameter.py
2. **expected output:**
   1. training logs for each hyperparameter setting
   2. Part 4 figures/tables:
      1. flower_perceptual_hyperparameter_table.png: summary table of test MSE and perceputal loss of different hyperparameters
      2. flower_mse_comparison_qualitative.png: comparison of the mse weight of the losses
      3. flower_perceptual_layer_qualitative.png: comparison of using different perceptual layers
      4. flower_learning_rate_qualitative.png: comparison of using different learning rates
      5. flower_epoch_qualitative.png: comparison of multiple epochs of one experiment

The other is Oxford 102 flower with AlexNet VGG19.
1. **command:** python3 alex_flower_hyperparameter.py
2. **expected output:**
   1. training logs for each hyperparameter setting
   2. Part 4 figures/tables:
      1. AlexNet_flower_perceptual_hyperparameter_table.png: summary table of test MSE and perceputal loss of different hyperparameters
      2. AlexNet_flower_mse_comparison_qualitative.png: comparison of the mse weight of the losses
      3. AlexNet_flower_perceptual_layer_qualitative.png: comparison of using different perceptual layers
      4. AlexNet_flower_learning_rate_qualitative.png: comparison of using different learning rates
      5. AlexNet_flower_epoch_qualitative.png: comparison of multiple epochs of one experiment
   
